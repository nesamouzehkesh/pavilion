{% extends "::twig/form.html.twig" %}

{% block body %}
    {{ form_errors(form) }}
    {{ form_row(form.title) }}
    {{ form_row(form.price) }}
    {{ form_row(form.image) }}
    {{ form_row(form.available) }}
    {{ form_row(form.description) }}
    {{ form_row(form.images) }}
{% endblock %}