{% if saman_displayFullView %}
    <!-- Navigation -->
    <nav class="navbar navbar-custom navbar-fixed-top" role="navigation">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-main-collapse" aria-expanded="false">
                    <span class="sr-only">Toggle navigation</span>
                    <i class="fa fa-bars"></i>
                </button>
                <a class="navbar-brand page-scroll" href="#page-top">
                    <i class="fa fa-play-circle"></i>  <span class="light">Zibaf</span>
                </a>
            </div>

            <div class="collapse navbar-collapse navbar-left navbar-main-collapse">
            {% if isNotIndex %}
                <ul class="nav navbar-nav">
                    <li><a href="{{ base_urls }}">{{ 'icon.home'|icon }} Home</a></li>
                </ul>    
            {% endif %}
            </div>

            <div class="collapse navbar-collapse navbar-right navbar-main-collapse">
                <ul class="nav navbar-nav navbar-right">
                    <li class="hidden"><a href="#page-top"></a></li>
                    {% if isNotIndex %}
                        <li><a href="{{ base_urls }}/order">Order</a></li>
                        <li><a href="{{ path('saman_shopping_product_home') }}">Gallery</a></li>
                        <li><a href="{{ base_urls }}/aboutus">About</a></li>
                        <li><a href="{{ base_urls }}/contact">Contact</a></li>
                    {% else %}
                        <li><a class="page-scroll" href="#order">Order</a></li>
                        <li><a class="page-scroll" href="#gallery">Gallery</a></li>
                        <li><a class="page-scroll" href="#about">About</a></li>
                        <li><a class="page-scroll" href="#contact">Contact</a></li>
                    {% endif %}
                    {% if app.user %}
                        <li class="dropdown hidden-xs">
                            {% set user = app.user %}
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true">
                                {{ 'customer.icon.myProfile'|icon }} {{ 'customer.profile'|trans }}
                                <span class="caret"></span>
                            </a>
                            <ul class="dropdown-menu">
                                <li><a href="{{ path('saman_shopping_customer_profile') }}">{{ 'customer.icon.myProfile'|icon }} {{ user.firstName }} {{ user.lastName }}</a></li>
                                <li><a href="{{ path('saman_shopping_order_add') }}">{{ 'order.icon.makeOrder'|icon }} {{ 'order.action.makeOrder'|trans }}</a></li>
                                <li><a href="{{ path('saman_shopping_orders') }}">{{ 'order.icon.myOrders'|icon }} {{ 'order.myOrders'|trans }}</a></li>
                                <li role="separator" class="divider"></li>
                                <li><a href="{{ path('saman_logout_web') }}">{{ 'icon.logout'|icon }} {{ 'action.logout'|trans }}</a></li>
                            </ul>
                        </li>
                        <li class="visible-xs"><a href="{{ path('saman_shopping_customer_profile') }}">{{ 'customer.icon.myProfile'|icon }} My Profile</a></li>
                        <li class="visible-xs"><a href="{{ path('saman_shopping_orders') }}">{{ 'order.icon.myOrders'|icon }} {{ 'order.myOrders'|trans }}</a></li>
                        <li class="visible-xs"><a href="{{ path('saman_logout_web') }}">{{ 'icon.logout'|icon }} {{ 'action.logout'|trans }}</a></li>
                    {% else %}
                        <li><a href="{{ path('saman_login_web') }}">{{ 'icon.logIn'|icon }} {{ 'word.logIn'|trans }}</a></li>
                        {#<li><a href="{{ path('saman_shopping_customer_joinus') }}">{{ 'icon.createAccount'|icon }} {{ 'word.createAccount'|trans }}</a></li>#}
                    {% endif %}                
                </ul>
            </div>
        </div>
    </nav>
{% else %}                    
    <nav class="navbar">
        <div class="container-fluid">
            <!-- Brand and toggle get grouped for better mobile display -->
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="#">Saman</a>
            </div>
            <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                <ul class="nav navbar-nav">
                    <li><a href="{{ path('saman_cms_page_index') }}">{{ 'icon.home'|icon }} CMS Home</a></li>
                    <li><a href="{{ app.request.getSchemeAndHttpHost() }}/front">AngularJS & Symfony</a></li>
                    <li><a href="{{ app.request.getSchemeAndHttpHost() }}/api/doc">Symfony API Doc</a></li>
                </ul>
                <ul class="nav navbar-nav navbar-right">
                    {% if app.user %}
                        <li class="dropdown hidden-xs">
                            {% set user = app.user %}
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true">
                                {{ 'customer.icon.myProfile'|icon }} {{ 'customer.profile'|trans }}
                                <span class="caret"></span>
                            </a>
                            <ul class="dropdown-menu">
                                <li><a href="{{ path('saman_shopping_customer_profile') }}">{{ 'customer.icon.myProfile'|icon }} {{ user.firstName }} {{ user.lastName }}</a></li>
                                <li><a href="{{ path('saman_shopping_order_add') }}">{{ 'order.icon.makeOrder'|icon }} {{ 'order.action.makeOrder'|trans }}</a></li>
                                <li><a href="{{ path('saman_shopping_orders') }}">{{ 'order.icon.myOrders'|icon }} {{ 'order.myOrders'|trans }}</a></li>
                                <li role="separator" class="divider"></li>
                                <li><a href="{{ path('saman_logout_web') }}">{{ 'icon.logout'|icon }} {{ 'action.logout'|trans }}</a></li>
                            </ul>
                        </li>
                        <li class="visible-xs"><a href="{{ path('saman_shopping_customer_profile') }}">{{ 'customer.icon.myProfile'|icon }} My Profile</a></li>
                        <li class="visible-xs"><a href="{{ path('saman_shopping_orders') }}">{{ 'order.icon.myOrders'|icon }} {{ 'order.myOrders'|trans }}</a></li>
                        <li class="visible-xs"><a href="{{ path('saman_logout_web') }}">{{ 'icon.logout'|icon }} {{ 'action.logout'|trans }}</a></li>
                    {% else %}
                        <li><a href="{{ path('saman_login_web') }}">{{ 'icon.logIn'|icon }} {{ 'word.logIn'|trans }}</a></li>
                        {#<li><a href="{{ path('saman_shopping_customer_joinus') }}">{{ 'icon.createAccount'|icon }} {{ 'word.createAccount'|trans }}</a></li>#}
                    {% endif %}                     
                    <li><a href='{{ path('saman_app_display_dashboard') }}'>{{ 'icon.setting'|icon }} Admin</a></li>
                </ul>
            </div>
        </div>
    </nav>
{% endif %}